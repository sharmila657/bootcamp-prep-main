// YOUR CODE BELOW
debugger

function myUnshift(array, value) {
    let newArray = [value];
    for (let i = 0; i < array.length; i++) {
        let element = array[i]
        newArray.push(element);
    
    }
    return newArray;
}

















